# Replace the body of decide(...) with your real decision logic later.
def decide(bar: dict, history: list) -> dict:
    # bar keys: symbol, ts, open, high, low, close, volume
    # Return {} to skip, or:
    # {"side":"buy","confidence":0.12,"tp_pct":0.02,"sl_pct":0.01}
    return {}  # start with no trades until we confirm wiring
